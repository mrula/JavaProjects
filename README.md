# JavaProjects
This repository contains my personal Java projects

The user must first install Java.

To run the interactive CV, first compile and then run by writing the following in the command-line:
```
javac InterCV.java
java InterCV.java
```
